import Login from "@/components/Login/Login";
import React from "react";

export const metadata = {
  title: "Todo | Login or Register",
};

const page = () => {
  return <Login />;
};

export default page;
